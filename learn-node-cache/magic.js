console.log('Required by ' + module.parent.filename);

// delete require.cache[__filename];
// If you uncomment above line then executing index.js will cause to load magic.js twice as you are deleting the cache trying to access it again via `util.js` file