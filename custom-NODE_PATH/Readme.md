Custom node path example:

```bash
NODE_PATH=a/node_modules/ node app.js
NODE_PATH=./a/node_modules/ node app.js # Semantically same as above.
```

```bash
export NODE_PATH=a/node_modules/
node app.js
```
